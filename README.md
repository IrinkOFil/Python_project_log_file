# Python_project_log_file
This repo contains a Jupyter notebook with parsing a log file

Objectives:
- Use regex to parse a log file
- Append and modify values in a dictionary
- Write to a file in CSV format
